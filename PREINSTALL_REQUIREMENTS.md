# Preinstall Requirements - AllAroundMe

Node.js 20 and MySQL come from the Multidev base image. No extra OS packages.

(Voice transcription happens in the companion Eshkolot-alphon app, which declares
ffmpeg in its own contract — not here.)

```vps-requirements
{
  "apt": []
}
```
